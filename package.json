{
  "name": "files",
  "version": "1.0.0",
  "description": "A complete expert system that simulates a virtual doctor. Built with SWI-Prolog (inference engine) + Node.js (bridge API) + React (frontend).",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
